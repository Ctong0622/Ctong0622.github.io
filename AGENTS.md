# AGENTS.md

1. Only plain HTML, CSS, and JavaScript. Never add a framework or a library.

2. Keep stylesheets in `css/` and images in `images/`.

3. Use lowercase filenames with hyphens and no spaces.

4. Keep the website responsive and make sure it works on desktop and mobile.

5. If a request requires a server, backend, database, or other feature outside this project, stop, tell me, and add it to `Later` in `PRD.md` instead.
